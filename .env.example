# Required on Vercel and any persistent deployment
# Use a PostgreSQL/Neon connection string.
DATABASE_URL=
HIRMAND_ADMIN_KEY=

# Optional auth configuration for the existing Better Auth layer
VITE_AUTH_ENABLED=false
